# Changelog

本文档记录 ai-chat-outline 的重要更新。

## [v2.8.4] - 2026-08-12

### 修复

- 修复 ChatGPT 页面滚动后目录条目顺序逐渐错乱，导致点击跳转到错误位置的问题。
- 稳定消息快照改为使用首次确认后的固定序号排序，避免继续依赖 ChatGPT 虚拟列表中会动态变化的 `observedTop`。
- 后续滚动扫描只更新已挂载条目的真实锚点，不再改变已有目录条目的顺序。
- 降低文本签名兜底去重的优先级，避免相似或重复提问覆盖更可靠的消息 ID、远程序号和原生目录序号。
- 更换 ChatGPT 稳定快照缓存键，避免读取 v2.8.3 可能已经写入的乱序缓存。

### 调试

- `__aiTocDebug()` 新增 `stableNextOrder`，用于查看稳定目录序号的下一个分配值。

## [v2.8.3] - 2026-08-11

### 修复

- 修复 ChatGPT 长对话虚拟列表滚动时，当前未挂载的目录条目被后续 DOM 扫描替换掉，导致条目反复缺失的问题。
- ChatGPT 目录新增稳定消息快照，按消息 ID、原生目录序号、远程序号、导航 ID 与文本签名合并已发现条目。
- 远程会话接口失败或只返回部分内容时，目录会保留已识别过的用户提问，避免滚动到其他位置后条目自动消失。
- 稳定消息快照按会话 ID 写入本地缓存，刷新或重新打开同一会话时可先用已识别目录补齐。

### 调试

- `__aiTocDebug()` 新增 `stableMessages` 与 `stableMessageContext`，用于确认 ChatGPT 目录是否由稳定快照补齐。

## [v2.8.2] - 2026-08-09

### 变更

- 项目更名为 `ai-chat-outline`。
- 核心脚本文件更名为 `ai-chat-outline.js`，并同步更新本地压力测试页引用。
- 更新用户脚本 `@name` 与 `@description`，使脚本元信息与新项目名一致。
- README 顶部描述改为中英文双语，说明侧边目录、长对话跳转、关键词过滤、回顶/到底、当前位置高亮和超长对话性能优化。

## [v2.8.1] - 2026-06-13

### 修复

- 修复 ChatGPT 原生目录分段挂载时清空完整远程会话数据，造成目录条目缺失的问题。
- 调整 ChatGPT 目录数据源优先级：完整会话数据优先，原生目录只用于绑定真实锚点。
- 完整会话数据尚未返回时，在原生目录与当前 DOM 中选择条目数量更多的数据源作为临时降级。
- 修复部分目录跳转将用户问题定位到顶部遮挡区域、视觉上只显示对应 AI 回答的问题。
- 精确跳转统一使用顶部安全间距，并在虚拟列表布局变化后持续校正真实用户消息位置。

## [v2.8.0] - 2026-06-08

### 新增

- 新增 Claude Web 基础支持，支持在 Claude 对话页面生成用户提问目录。
- 新增 Claude 站点适配器，识别 `[data-testid="user-message"]` 与 `.font-user-message` 等常见用户消息节点。
- 新增 Claude 气泡标签 `Cl`。

### 变更

- 脚本匹配范围新增 `https://claude.ai/*`。
- 脚本名称与说明更新为支持 Gemini、ChatGPT 与 Claude。

### 说明

- Claude 首版支持基于当前页面已挂载 DOM 生成目录，暂不包含类似 ChatGPT 的远程会话补全逻辑。

## [v2.7.0] - 2026-06-08

### 修复

- 修复 ChatGPT 长对话中由于页面只挂载部分会话 DOM，导致目录条目缺失或滚动到附近才出现的问题。
- 修复 ChatGPT 目录点击时可能无反应、跳到相邻用户提问或落到 AI 回复区域的问题。
- 修复基于可见文本顺序推断目标带来的误绑定问题，避免相似文本或懒加载占位导致错误跳转。

### 变更

- ChatGPT 目录完整性优先结合远程会话数据生成，不再只依赖当前已挂载 DOM。
- ChatGPT 跳转目标优先使用真实 `data-message-id`，其次使用 `data-turn-id-container` / `data-turn-id` 壳层唤醒挂载。
- 槽位兜底改为必须通过目标文本校验，避免仅凭位置索引接受错误 DOM。

### 调试

- 新增 `__aiTocVersion` 与 `__aiTocDebug()`，用于查看目录数量、远程会话数据、原生 Prompt 目录、最后一次点击和跳转路径。

## [v2.6.0] - 2026-04-26

### 新增

- 新增浮动目录面板的侧边吸附气泡折叠模式。
- 新增展开面板位置和折叠气泡位置的持久化保存。
- 新增闲置后自动折叠能力，并在搜索、悬停、聚焦和导航执行中保持展开。
- 新增搜索清空按钮和无结果状态。
- 新增站点专属气泡标签：ChatGPT 显示 `GPT`，Gemini 显示 `Gem`。

### 变更

- 展开动画改为侧边滑出过渡。
- 优化面板和气泡的视口边界约束。
- 优化折叠气泡内图标和文字的居中表现。

### 修复

- 修复刷新后右侧气泡展开时面板错误出现在左侧的问题。
- 修复右侧折叠动画因宽度参与定位计算导致的抖动问题。

## [v2.5.0] - 2026-04-19

### 重构

- 重构 `AI-web-enhancer.js` 单文件内部结构，按站点适配、滚动导航、目录同步、UI 构建与启动入口进行分区
- 收敛重复的滚动容器、消息索引、目录节点获取逻辑，降低后续维护成本
- 标准化顶部 / 底部边界滚动配置，减少主流程中的匿名逻辑

### 修复

- 修复重构过程中遗留的编码问题，将脚本统一为 UTF-8 无 BOM
- 修复 `README.md` 与 `CHANGELOG.md` 中的乱码内容
- 继续稳定 ChatGPT / Gemini 下的目录点击、高亮同步、滚动跟随与边界导航逻辑

### 优化

- 优化 `scanContent()` 的采集、渲染与同步流程，使主路径更清晰
- 优化目录交互状态管理，减少点击锁定、手动高亮与滚动释放之间的重复判断

## [v2.4.0] - 2026-03-26

### 新增

- 为纯图片消息增加目录兜底标题，支持显示 `图片`、`图片 xN` 与 `图片：描述`
- 新增本地性能回归页 `stress-test.html`，用于验证长对话与大 DOM 场景

### 修复

- 修复 ChatGPT 中点击目录后页面跳转正确但高亮偶尔停留在上一条的问题
- 修复 Gemini 中点击目录后更容易落到 AI 回答区域而不是对应提问位置的问题
- 修复返回顶部过程中目录最后一次不同步的问题

### 优化

- 优化长对话与大体量 DOM 场景下的目录扫描、位置缓存与滚动同步性能
- 优化 `MutationObserver` 触发范围，减少无关 DOM 变化导致的重复扫描
- 优化目录跟随当前阅读位置的自动滚动体验

## [v2.3.0] - 2026-03-25

### 修复

- 修复 ChatGPT 中浏览 AI 长回复时目录高亮偶尔跳到最后一条问题
- 修复 Gemini 中点击回到顶部后页面与目录不同步的问题

### 优化

- 优化目录当前项识别逻辑
- 优化目录与页面滚动的同步体验
- 提升长对话场景下的目录定位稳定性

## [v2.2.0] - 2026-02-24

### 新增

- 支持 ChatGPT

### 优化

- 优化目录生成与跳转体验
- 提升长对话场景下的稳定性

## [v2.1.0] - 2026-02-03

### 新增

- 新增目录搜索功能

### 优化

- 优化目录显示与交互体验

## [v2.0.0] - 2026-01-29

### 新增

- 支持在 ChatGPT 与 Gemini 页面生成侧边目录
- 支持点击目录项快速跳转
- 支持回到顶部与直达底部

### 优化

- 优化长对话场景下的浏览效率

## [v1.4.0] - 2026-01-25

### 新增

- 支持拖拽改变侧边目录面板的位置，避免遮挡网页原有的重要内容。
- 引入 `MutationObserver` 基础监听，尝试自动捕获新发送的对话。

### 优化

- 优化长文本截断逻辑，超出宽度的提问现在会优雅地显示为省略号。

## [v1.3.0] - 2026-01-20

### 优化

- 全面美化面板 UI 样式，引入圆角（Border Radius）与悬浮阴影（Box Shadow）过渡效果。
- 自定义目录列表的滚动条样式，使其更贴合站点的深色模式主题。

### 修复

- 修复浮动面板在小屏幕或特定分辨率设备上可能超出屏幕边界被截断的问题。

## [v1.2.0] - 2026-01-15

### 新增

- 新增“直达底部”快捷按钮，方便在长对话中一键定位到最新回复。

### 修复

- 修复页面动态加载历史对话时，目录无法自动刷新获取旧提问的问题。
- 修复部分特殊字符或全英文提问被提取为空白节点的问题。

## [v1.1.0] - 2026-01-10

### 新增

- 目录项目前支持高亮显示交互反馈（点击时背景色闪烁提示）。
- 为面板添加了固定的 z-index 层级，防止被页面的其他弹窗或导航栏遮挡。

### 优化

- 优化 DOM 提取效率，精简不必要的节点遍历操作。

## [v1.0.0] - 2026-01-05

### 新增

- 核心功能首发，支持在页面自动提取用户提问（`.query-text-line`）并生成侧边索引目录。
- 支持点击目录条目平滑滚动定位到对应对话区域。
- 适配深色模式主题基础样式。
